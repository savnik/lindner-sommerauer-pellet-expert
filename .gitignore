# Source PDFs and photos of manuals, controller screens and installations
# live OUTSIDE the repo tree. They are never committed here, scrubbed or not.
# The ingest-document skill reads them from wherever Peter keeps them.

# Installation records are private and live in the owner's working directory.
# Ignored here so one created while developing in this repo is never committed.
pellet-installations/

# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/
*.swp
*~

# Claude Code local state
.claude/settings.local.json

# Node / Python tooling, if any is ever added
node_modules/
__pycache__/
*.pyc
.venv/

# Logs and temp
*.log
*.tmp
